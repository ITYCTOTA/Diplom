# GameHub Diploma API

Backend is organized according to the diploma modules: auth, games, library, purchases, reviews, recommendations, profile, activity, groups, and friends.

```bash
cp .env.example .env
npm install
npm run db:schema
npm run db:seed
npm run dev
```

The API is available at `http://localhost:4000/api`.
